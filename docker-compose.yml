version: "3.9"

services:
  mentor-pathways-guide:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: mentor-pathways-guide

    ports:
      - "3004:3000"

    environment:
      NODE_ENV: production
      PORT: 3000

    restart: unless-stopped